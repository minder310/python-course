title: python資料分析
description: 學習基本python 和基礎資料分析。
template:
  repo: python-template
  name: python
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
